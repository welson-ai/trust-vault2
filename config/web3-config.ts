// Minimal config stub for deployment compatibility
// MetaMask connection is handled directly in components without this config

export const projectId = process.env.NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID || ""
